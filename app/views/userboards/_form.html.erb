<%= form_for(@userboard) do |f| %>
  <% if @userboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@userboard.errors.count, "error") %> prohibited this userboard from being saved:</h2>

      <ul>
      <% @userboard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
