<div class="middle-box text-center loginscreen  animated fadeInDown">
  <div>
      <div>
          <h1 class="logo-name">S90</h1>
      </div>
      <h3>Welcome to S90</h3>
      <p>Login in. To see it in action.</p>
      <!-- <form class="m-t" role="form" action="#"> -->
          <%= form_for(resource, as: resource_name, class: "m-t", url: session_path(resource_name)) do |f| %>
            <div class="form-group">
                <!-- <input type="email" class="form-control" placeholder="Username" required=""> -->
                <%= f.email_field :email, autofocus: true, placeholder: "Email",class: "form-control" %>
            </div>
            <div class="form-group">
                <!-- <input type="password" class="form-control" placeholder="Password" required=""> -->
                <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "Password" %>
            </div>
            <!-- <button type="submit" class="btn btn-primary block full-width m-b">Login</button> -->
            <%= f.submit "Login", class: "btn btn-primary block full-width m-b" %>
          <%end%>
          <%= link_to "Forgot password?", forgot_password_path %>
          <p class="text-muted text-center"><small>Do not have an account?</small></p>
          <%= link_to "Create an account", new_user_registration_path, class: "btn btn-sm btn-white btn-block" %>
  </div>
</div>