'<%= form_for members_path do |f| %>'
	$(".department-select").html("<%= escape_javascript (render partial: 'department_autofilled_field', locals: { departments: @departments, f: f }) %>");
'<% end %>'
